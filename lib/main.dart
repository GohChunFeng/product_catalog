import 'package:product_catalog/app.dart';

// this entry point by default is using the dev env, you can change default env in the AppConfig file under app_base
void main() async {
  await runMyApp();
}